/**
 * DEPRECATED — src/server.ts
 *
 * SSR entry point has moved to src/server/entry.ts.
 * vite.config.ts now points to `server/entry` — this file is no longer loaded.
 *
 * Kept as a placeholder to prevent git diff confusion.
 * Safe to delete once the team is aligned.
 */
export {};
